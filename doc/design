# 蓝色警戒 Web 版 —— 游戏设计文档 v1

> 项目代号：fishgame
> 版本：v0.1（初稿）
> 日期：2026-09-07

---

## 1. 概述

### 1.1 定位

模仿经典 RTS《蓝色警戒》（State of War, Cypronia, 2001）的 Web 即时战略游戏。保留其最核心的差异化设计——**"占领即生产"**：玩家没有传统建造系统，而是操控一艘核心飞碟去占领地图上的中立/敌方建筑，占领后建筑自动为你生产单位。

### 1.2 设计支柱

| 支柱       | 说明                                              |
| ---------- | ------------------------------------------------- |
| 占领即生产 | 唯一的生产途径是占领建筑，飞碟是全局命脉          |
| 决策制胜   | 弱化微操（APM），强调扩张/防守/兵力分配的宏观决策 |
| 快节奏     | 单局 15~25 分钟，无冗长发育期                     |
| 兵种克制   | 坦克/防空/远程/空中构成克制链，混编才能取胜       |

### 1.3 平台与技术约束

- 纯浏览器运行，桌面端为主（键鼠操作），暂不考虑移动端
- 技术栈：Vue 3 + TypeScript + Vite（已搭建）
- 渲染：Canvas 2D（俯视角），游戏逻辑与 UI 框架解耦

---

## 2. 世界观与背景

2020 年，七洲联盟建立的"超脑"全球防御系统被宗教势力**水晶兄弟会（Beho-Sunns）**入侵控制，22 个军事基地沦陷。玩家扮演同盟军团指挥官，驾驶核心指挥飞碟逐个夺回基地。

> v1 不做剧情演出，背景仅用于关卡命名与开场文案。

---

## 3. 核心玩法循环

```
 探索地图 → 发现中立建筑 → 派飞碟占领 → 建筑自动产兵
      ↑                                        ↓
 混编部队 ←—— 生产数量达标触发升级 ←—— 获得资源（金矿）
      ↓
 进攻敌方据点 / 防守己方建筑（敌 AI 会反扑占领）
```

- **胜负条件**：占领地图上所有敌方/中立核心建筑（指挥部）即胜利；己方飞碟被击毁且无法重生则失败。
- **单局时长目标**：15~25 分钟。

---

## 4. 核心系统

### 4.1 飞碟（玩家核心单位）

| 属性 | 数值（初版）                                             |
| ---- | -------------------------------------------------------- |
| HP   | 800（可随关卡成长）                                      |
| 移动 | 全图自由飞行，无视地形                                   |
| 占领 | 停留在建筑上方 **30 秒**完成占领（有进度条，受击不打断） |
| 武器 | 专用炮：仅能攻击空中单位（敌飞碟/空袭编队）与**防空坦克**；伤害固定（不做成长） |
| 重生 | 被击毁后 30 秒在己方最后指挥部重生，期间无法生产         |

**武装规则**：

- 飞碟移动速度固定（不随关卡/升级变化）；
- 飞碟配备专用炮，弹道速度固定，可攻击目标为：**空中单位**（敌方飞碟、空袭编队）与**防空坦克**（唯一的可攻击地面单位）；
- 对其余地面单位、所有建筑**均无法开火**（经济/生产建筑必须占领获取，其他地面战斗必须由己方部队执行）；
- 伤害为固定值，不随关卡/升级成长；
- **伤害基准**：2 次攻击消灭 1 辆初级防空坦克（即初级防空坦克 HP = 2 × 飞碟单发伤害，以此为数值锚点）；
- 飞碟能打防空坦克的设计意图：防空坦克是飞碟的天敌（对空火力），飞碟必须拥有反制窗口，否则防空坦克成为飞碟的绝对克制而无解。

**目标选择优先级**（自动索敌规则）：

1. **敌方飞碟**（最高优先，触发 MAD 对决）；
2. 敌方空袭编队；
3. 威胁范围内的敌方防空坦克。

- 即：可攻击范围内同时出现敌方飞碟与其他目标时，飞碟**必须优先攻击敌方飞碟**（无手动覆盖）；
- 飞碟对防空坦克之外的地面单位无威胁，飞碟与地面部队的交战界面清晰：**防空坦克打飞碟，飞碟反打防空坦克**，其余互不干涉。

**飞碟对决（MAD 机制）**：

- 两方飞碟互相攻击时，**同时被摧毁**（相互确保摧毁）；
- 实现规则：当 A 方飞碟的炮弹命中 B 方飞碟、且 B 方飞碟也已锁定 A 并开火（或弹道已在途中）时，双方立即同时阵亡；
- 战术含义：
  - 飞碟对决没有"操作取胜"空间，先手偷袭（对方未开火时击落）是唯一优势情形；
  - 用己方飞碟换掉敌方飞碟是可行的战术交换（换取对方生产力中断 30 秒）；
  - 用侦察类空袭提前探明敌方飞碟动向，避免己方飞碟被先手偷袭，是高阶玩法。

**规则**：

- 飞碟同一时间只能占领或升级一个建筑；
- **占领双路径（已定案）**：建筑可通过两种方式被夺取——① 飞碟停靠读条 30 秒；② **部队攻击将建筑血量清零**（清零瞬间建筑易主，而非被摧毁）；
- **升级只能由飞碟执行**（部队无法升级建筑）；
- 玩家建造的防御塔**不可被占领**、只能被摧毁（金币投资不会被白嫖）；
- 飞碟被摧毁 = 生产力中断，是全局最大风险点（敌方飞碟的 MAD 对决、防空火力是主要威胁）。

**建筑升级（已定案）**：

- 建筑被占领后为 **1 级**，可由**飞碟停靠升级**（与占领同交互：停留读条）；
- **受击不打断**：飞碟被攻击**不会打断**占领/升级进程（飞碟只是掉血，读条继续；飞碟被摧毁才会中断）；
- 升级花费时间（v1 数值：**每级 20 秒**），升级期间飞碟被占用、有进度条；
- 建筑等级上限 **3 级**，效果随等级线性提升：

| 建筑类型        | 升级效果                                                                    |
| --------------- | --------------------------------------------------------------------------- |
| 兵营 / 战车工厂 | 生产速率 +50%/级                                                            |
| 金矿            | 产出 +50%/级                                                                |
| 太阳能搜集器    | 电力供给 +50%/级                                                            |
| 空军基地        | 空袭点数积攒速率 +50%/级                                                    |
| 防空炮          | 射速 +30%/级                                                                |
| 指挥部          | 升级解锁全部建筑等级上限（指挥部 1 级时其他建筑上限 2 级，3 级时上限 3 级） |

- 建筑失守后等级**重置**（重新占领从 1 级开始，敌方 AI 同理）——攻防拉锯的代价真实存在；
- 升级是飞碟的**时间投资**：飞碟在"扩张占新点"与"深耕升级已有建筑"之间存在节奏权衡，且升级途中暴露在风险中。

### 4.2 资源系统

| 资源           | 来源               | 用途                                 |
| -------------- | ------------------ | ------------------------------------ |
| **资金（金）** | 占领金矿后持续产出 | 单位生产消耗（自动扣款）；购买防御塔 |
| **电力**       | 占领太阳能搜集器   | 建筑运转、生产速率加成               |

- 占领金矿后每 2 秒产出 10 金，产出速率随占领数量线性叠加；
- 电力不足时生产速率减半（鼓励玩家优先抢占电力点）；
- 生产的本质是"自动消耗资金排队"，玩家通过**占领更多工厂/兵营**来增加并行生产队列。

**全自动生产（已定案）**：

- 生产队列**全自动**运行：资金充足时自动扣款并生产，玩家不可暂停、不可调整队列顺序；
- 玩家对生产的影响手段只有一个——**占领或失去生产建筑**（这是"占领即生产"设计的自然延伸，把经营压力全部转化为地图控制权争夺）；
- 各建筑有独立的内置循环队列（如兵营按 步兵→机器人→步兵 循环），产能速率由电力决定。

### 4.3 建筑体系

**地图预置建筑**（占领获取，不可新建）：

| 建筑         | 功能                             | 备注            |
| ------------ | -------------------------------- | --------------- |
| 指挥部       | 核心建筑，胜负判定点             | 每图 2~4 个     |
| 兵营         | 自动生产步兵/机器人              | 便宜、产能快    |
| 战车工厂     | 自动生产五型坦克；工厂分**初级/中级/高级**三档 | 主力产能        |
| 空军基地     | 积攒空袭点数、解锁空袭召唤       | 见 4.5 空袭系统 |
| 金矿         | 持续产出资金                     | 必争点          |
| 太阳能搜集器 | 产出电力                         | 产能保障        |
| 防空炮       | 预置防御建筑，拦截空袭编队与飞碟 | 守卫关键区域    |

**可建造建筑——防御塔（已定案）**：

玩家唯一的建造手段，使用**金币购买**：

| 防御塔 | 价格   | 功能                       |
| ------ | ------ | -------------------------- |
| 对地塔 | 200 金 | 自动攻击范围内敌方地面单位 |
| 防空塔 | 150 金 | 自动攻击敌方飞碟与空袭编队 |

**部署规则**：

- **只能部署在己方建筑一定范围内**（半径 5 格，以任一己方建筑为圆心）；
- 单座己方建筑周边最多部署 3 座防御塔（防塔海）；
- 支付金币后 10 秒建造完成（建造中可被攻击摧毁，金币不返还）；
- 防御塔**不可被占领**、只能被摧毁（杜绝"白嫖"玩家金币投资）；
- 己方建筑失守后，其周边防御塔保留并继续战斗（v1 简化：塔独立存在直到被摧毁）。

**战术定位**：

- 防御塔是金币的**主动防御性消费**（与空袭点数的进攻性形成对照）；
- 补齐指挥制下"无法微操防守"的空缺：用金币在关键位置固化防御力量；
- 占领新建筑 = 扩大防御塔可部署范围，进一步强化"占领即生产"的扩张激励。

**占领规则**：

- 所有地图预置建筑均有血量（含防空炮）；部队不会攻击己方建筑（同阵营不互射）；
- **路径一（飞碟占领）**：飞碟停靠读条 30 秒，无需战斗；
- **路径二（武力夺取）**：部队将敌方/中立建筑血量清零 → 建筑立即易主（血量回复至满值），**不是摧毁**；
- 中立建筑同样可被部队打空夺取（比飞碟读条快，但需要兵力）；
- 建筑易主后等级重置（重新从 1 级开始）；
- 敌方 AI 同样会派兵/飞碟抢占中立和己方建筑（核心压力来源）。

**双路径的设计意图**：

- 飞碟占领 = **无成本但慢**（30 秒占用飞碟）；武力夺取 = **快但耗兵**（顶着防守火力打建筑）；
- 指挥制下部队自动交战，"顺手打掉建筑"让进攻部队天然具备夺点能力，无需飞碟往返奔波；
- 升级被飞碟独占，保证飞碟在和平期也有核心用途。

### 4.4 单位体系与克制链

**没有可生产的空中单位**（已定案）：空中的威胁只有双方的飞碟和召唤而来的空袭编队，空中打击能力全部由空袭系统（4.5）承担。

**地面单位**：

- **兵营生产**：

| 单位   | 定位           | 克制           | 被克制           |
| ------ | -------------- | -------------- | ---------------- |
| 步兵   | 便宜填线、占点 | 无             | 一切范围伤害     |
| 机器人 | 高攻低防突击   | 坦克           | 防御塔群、步兵海 |

- **战车工厂生产（坦克体系）**：

**工厂档位（已定案）**：战车工厂分为**初级 / 中级 / 高级**三档（地图预置属性，非升级产生），档位决定该工厂**可生产坦克的单位等级上限**：

| 工厂档位 | 可产坦克等级 | 说明 |
|---|---|---|
| 初级工厂 | 1 级坦克 | 基础产能 |
| 中级工厂 | 最高 2 级坦克 | 中期核心争夺点 |
| 高级工厂 | 最高 3 级坦克 | 稀缺，通常重兵把守 |

- **坦克单位等级**（与既有升级系统统一，上限 3 级）：单位属性随等级提升（每级 +15%），高级工厂产出的坦克出生即为高等级；
- 单位的等级**出生锁定**：初级工厂产出的坦克不会后天升级（升级系统只作用于建筑，不作用于单位——单位等级由产出它的工厂档位决定）；
- 玩家的兵种质量策略 = **争夺高等级工厂**：地图上高级工厂数量稀缺（每图 1~2 座），是设计好的核心争夺点；
- 工厂档位不可改变（不可升级初级→高级，档位是地图设计的一部分）；工厂被夺取后按原档位为敌方生产。

**五型坦克**：

| 坦克类型 | 定位与特性 | 克制 | 被克制 |
|---|---|---|---|
| 普通坦克 | 正面主力、高甲均衡 | 机器人、步兵 | 榴弹炮、防御塔、火焰坦克（近身） |
| 火焰坦克 | **范围攻击**（同时命中约 2 个单位的溅射），**攻击距离略近**，需贴脸输出 | 步兵海、机器人集群 | 普通坦克（对拼吃亏）、榴弹炮远程 |
| 榴弹炮 | **超远程**火力、**脆皮**（HP 低于普通坦克一半） | 普通坦克、建筑、防御塔 | 一切近战突击、火焰坦克贴脸 |
| 防空坦克 | 对空专用、**飞碟天敌** | 飞碟、空袭编队 | 飞碟（2 发被消灭）、所有地面单位 |
| 激光坦克 | **伤害随时间增加**（对同一目标持续照射，伤害线性递增），**攻击距离最近** | 长时间对峙的高价值目标（防御塔、榴弹炮） | 一切近战突击、被快速集火 |

**坦克体系设计要点**：
- 五型坦克构成"距离-伤害"谱系：**激光（最近/越打越疼）→ 火焰（近/溅射）→ 普通（中/均衡）→ 防空（中/对空）→ 榴弹炮（最远/最脆）**；
- 榴弹炮是唯一远程拆建筑/塔的手段，但需要前排保护；激光坦克是消耗战之王但交战距离吃亏、怕被秒；
- 全自动生产下，玩家通过**占领更多战车工厂**（并行队列）而非选兵种来影响兵种结构（各工厂内置循环队列覆盖全部五型）。

**克制数值原则**：被克制方受到 1.5~2 倍伤害，克制方受到 0.6 倍伤害。

### 4.5 空袭系统（召唤制）

**没有可操控的空军单位**，空袭是"占领空军基地 → 积攒点数 → 召唤空袭"的技能系统：

**点数积攒**：

- 每占领一座空军基地，每 3 秒自动获得 1 点对应类型的空袭点数（多座基地叠加积攒速率）；
- 失去空军基地则停止积攒（已积攒的点数保留）；
- 点数上限 20 点/类型，防止无限囤积。

**空袭类型**（v1 提供两种）：

| 空袭     | 消耗  | 效果                                         | 反制手段                     |
| -------- | ----- | -------------------------------------------- | ---------------------------- |
| 轰炸空袭 | 10 点 | 对目标区域投弹，大范围高额伤害（对建筑减半） | 防空炮拦截、飞碟拦截         |
| 侦察空袭 | 5 点  | 侦察机扫过指定区域，揭示沿途视野 15 秒       | 防空炮击落（仅损失视野收益） |

**召唤规则**：

- 玩家点击 HUD 空袭按钮 → 选择目标点 → 编队从最近的己方空军基地起飞，沿直线飞向目标；
- 空袭编队是**一次性的不可控实体**（非单位）：轰炸编队到达目标投弹后返场消失；侦察机到达终点后消失；
- 空袭编队飞行途中可被**防空炮**（优先目标）和**飞碟**（反空炮）击落，击落则空袭落空、点数白费；
- 敌方 AI 使用相同的空袭系统（AI 的空袭偏好打击玩家建筑密集区）。

**战术定位**：

- 空袭 = 花费时间（占基地攒点）换来的战略打击，而非即时操作；
- 防空炮因此成为守卫要地的核心防御，反空袭博弈（诱导敌方空袭到防空密集区）是高阶玩法。

### 4.6 升级系统（简化版）

- **单位升级 = 工厂档位制**（v1 已定案）：坦克等级由产出工厂的档位决定（初级=1 级/中级=2 级/高级=3 级），出生锁定、不可后天升级；
- **建筑升级 = 飞碟升级制**：见 4.1 建筑升级规则（上限 3 级，每级 20 秒）；
- 原设计"按累计生产数量升级单位"已废弃（与出生锁定冲突，且工厂档位制已提供质量成长曲线）；
- 空袭点数积攒速率不参与升级；v1 不做分支科技树。

### 4.7 敌方 AI（v1 简化）

- 状态机：`防守 → 试探扩张 → 抢占中立点 → 集团进攻`；
- AI 拥有自己的飞碟、生产建筑与空军基地，遵循与玩家完全相同的规则（含空袭点数积攒、防御塔购买）；
- 难度分级（3 档）调整：AI 初始部队规模、扩张频率、资源加成。

---

## 5. 操作设计（Web 键鼠，指挥制）

**操作理念（已定案）——指挥制**：玩家扮演的是"军团指挥官"而非"操作员"。除飞碟外**不能选中/操控单个作战单位**，部队行为完全由 AI 自动执行，玩家只下达宏观指令。

### 5.1 可下达的指令类型

| 指令对象     | 可下达指令                                                                                | 说明                             |
| ------------ | ----------------------------------------------------------------------------------------- | -------------------------------- |
| **飞碟**     | 完全可控：移动、攻击目标、占领建筑                                                        | 唯一可直接操控的实体             |
| **集结点**   | 设置/移除多个集结点；各生产建筑可绑定不同集结点                                           | 见 5.2 多集结点机制              |
| **生产建筑** | 单独指令：设定集结点绑定 / 调整出兵倾向（如"防御模式"驻守周边 vs "进攻模式"随集结点推进） | 兵营/战车工厂/空军基地可各自下达 |
| **空袭**     | 选目标点召唤（B/V）                                                                       | 见 4.5                           |

### 5.2 多集结点机制（已定案）

- 玩家可在地图上设置**多个集结点**（v1 上限 8 个，可随时增删）；
- 新产出的单位**自动选择距离最近的集结点**作为目标（按生产建筑位置计算直线/路径距离）；
- 每个生产建筑可**绑定指定集结点**（覆盖"最近"逻辑，用于定向支援某条战线）；
- 移除集结点后，原本趋向它的部队按"最近"规则自动重新分流到其余集结点；全部移除时回退为驻守所属建筑。

**部队自动行为（AI 托管）**：

- **无集结点时**：部队驻守其所属建筑周边（防守半径），自动攻击进入范围的敌人；
- **有集结点时**：部队自动向绑定的集结点移动，途中遇敌自动交战（攻击-移动），到达后转入驻守；
- **威胁响应**：己方建筑受攻击时，附近部队自动增援（无需玩家操作）；
- 玩家**不能**框选单位、不能指定攻击目标、不能控制单个单位的移动。

### 5.3 键鼠操作表

| 操作                   | 行为                                                                  |
| ---------------------- | --------------------------------------------------------------------- |
| 左键点击飞碟           | 选中飞碟（唯一可选中单位）                                            |
| 右键（选中飞碟时）     | 移动 / 攻击目标 / 占领建筑                                            |
| Shift + 左键点击空地   | **添加集结点**（画布/小地图均有效）                                   |
| 点击已有集结点         | 选中集结点（可删除、可绑定生产建筑）                                  |
| 左键点击己方生产建筑   | 打开建筑指令面板（绑定集结点/攻防倾向）                               |
| 空格                   | 跳转最近警报（己方建筑被攻击）                                        |
| B / V                  | 召唤轰炸空袭 / 侦察空袭（进入目标点选模式）                           |
| T / Y                  | 购买对地塔 / 防空塔（进入部署点选模式，仅高亮己方建筑范围内有效区域） |
| ESC                    | 取消空袭/防御塔点选 / 取消选择                                        |
| 小地图点击             | 移动镜头；Shift+点击 添加集结点                                       |
| 滚轮 / WASD / 屏幕边缘 | 镜头控制                                                              |

**由此简化的内容**：无编队系统（`1~9`）、无单位框选、无单位微操；寻路与战斗全部自动。

**设计收益**：

- 把 RT S的 APM 门槛降为零，胜负完全取决于**战略决策**（往哪集结、占哪个点、何时换家）；
- 与"占领即生产""全自动生产"共同构成纯指挥层体验，三条设计一脉相承。

---

## 6. 关卡结构

- v1 提供 **3 张关卡**（战役第一章）：
  1. **夺回前哨**：教学关，1 个敌方指挥部，无空袭；
  2. **金矿争夺**：中立金矿密集，AI 高频抢占；
  3. **要塞攻坚**：敌方重防+拥有空军基地（会召唤空袭），需防空炮掩护推进。
- 每关地图尺寸约 64×64 格（tile 40px，画布 1280×720）。
- 部分关卡有**时间锁**：超时后敌方无限增援（还原原作压力感，v1 第 3 关启用）。

---

## 7. 技术方案

### 7.1 架构分层

```
src/
├── game/                  # 纯 TS 游戏引擎层（不依赖 Vue）
│   ├── core/              # 游戏循环、事件总线、指令系统
│   ├── ecs/               # 实体-组件（Unit/Building/Projectile 轻量实现）
│   ├── systems/           # 移动寻路、战斗、生产、占领、AI、资源
│   ├── map/               # 地图数据、tile、寻路网格
│   └── render/            # Canvas 2D 渲染器、镜头、特效
├── ui/                    # Vue 组件（Hud、菜单、编队面板）
├── stores/                # Pinia：UI 状态（选中、资源快照）
└── levels/                # 关卡数据（JSON）
```

**关键决策**：

- 游戏逻辑用固定时间步长（60Hz）更新，渲染插值，与 Vue 响应式完全隔离（性能考虑，单位数可达 200+）；
- Vue 只负责 HUD/菜单，通过事件总线 + Pinia 快照读取游戏状态（每帧节流同步）；
- 寻路：网格 A\*（v1 不做流场，单位间碰撞用简单推挤分离）；
- 不引入渲染引擎依赖，MVP 用 Canvas 2D + 程序化图形，**美术方向采用几何图形风（已定案）**：
  - 单位 = 基本几何形状组合（如坦克=矩形车体+圆形炮塔、飞碟=椭圆+光圈、步兵=小圆点）；
  - 阵营用颜色区分（玩家蓝、敌方红、中立灰），单位类型靠形状+图标区分；
  - 特效走简约路线：纯色描边爆炸圈、渐隐弹道线；
  - 后续如需升级美术，仅替换渲染层图形定义，不影响游戏逻辑。

### 7.2 性能预算

- 同屏单位 ≤ 300，逻辑帧 ≤ 2ms，渲染帧 ≤ 8ms（60fps）。

---

## 8. 里程碑

| 阶段            | 内容                                           | 验收标准                                         |
| --------------- | ---------------------------------------------- | ------------------------------------------------ |
| **M1 核心原型** | 地图+飞碟移动+选择/指令+占领一座兵营并自动产兵 | 能在画布上完成一次"占建筑→出兵→打掉一座防御工事" |
| **M2 战斗闭环** | 全单位、克制伤害、敌方 AI、胜负判定            | 可完整打赢第 1 关                                |
| **M3 战役成型** | 3 关卡、升级系统、时间锁、音效占位             | 3 关全部可通                                     |
| **M4 打磨**     | 特效、平衡性调整、本地存档、教学提示           | 可对外试玩                                       |

---

## 9. v1 范围外（明确不做）

- 多人联机、阵营选择、科技树分支、海战、剧情动画、移动端适配。

---

## 10. 已定案决策记录

| #   | 决策     | 结论                                                                                                                                                     |
| --- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | --- |
| 1 | 飞碟武装 | 专用炮：仅可攻击空中单位（敌飞碟/空袭编队）与防空坦克（唯一可打的地面单位）；2 次攻击消灭初级防空坦克；索敌优先级：敌飞碟 > 空袭编队 > 防空坦克 |                          |     |
| 2   | 生产队列 | 全自动，玩家不可干预，只能通过占领/失守建筑影响生产                                                                                                      |
| 3   | 美术方向 | 几何图形风，颜色分阵营、形状分单位                                                                                                                       |
| 4   | 飞碟对决 | 移动/弹道速度固定；双方飞碟互相对射则同时被摧毁（MAD），先手偷袭是唯一优势                                                                               |
| 5   | 空中力量 | 取消可生产/可操控的空中单位；空袭改为占领空军基地积攒点数后召唤（一次性不可控编队）                                                                      |
| 6   | 操作方式 | 指挥制：仅飞碟可操控，其余单位全部 AI 托管；支持多个集结点（上限 8），单位默认流向最近集结点，生产建筑可定向绑定                                         |
| 7   | 防御塔   | 唯一可建造建筑：金币购买（对地塔 200 金/防空塔 150 金），只能部署在己方建筑半径 5 格内，每建筑限 3 座，不可被占领；预置炮塔已移除                        |
| 8 | 建筑升级 | 飞碟停靠花费时间升级建筑（上限 3 级，每级 20 秒）；占领 30 秒；受击不打断占领/升级（仅飞碟被摧毁才中断）；夺取后等级重置；指挥部等级决定全局建筑等级上限 |
| 9 | 占领双路径 | 飞碟读条占领 与 部队将建筑血量清零夺取 并存（清零=易主回满血，非摧毁）；升级仅飞碟可做 |
| 10 | 坦克体系 | 五型：普通（均衡）、火焰（范围溅射约 2 单位/近距离）、榴弹炮（超远程/脆皮）、防空（对空/飞碟天敌）、激光（伤害随时间递增/攻击距离最近） |
| 11 | 工厂档位 | 战车工厂分初级/中级/高级三档（地图预置、不可改变），决定产出坦克的等级上限（1/2/3 级）；单位等级出生锁定；单位按生产数量升级的旧规则已废弃 |     |

## 11. 待决问题（下次评审）

1. 是否需要困难模式的"AI 预判战术"（诱敌、假撤退）？
2. 单位阵亡是否返还部分资金（影响"损失容忍度"曲线）？
